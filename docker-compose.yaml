services:
  streamer:
    build: .
    env_file: .env.local
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes: 
      - ./config.toml:/app/config.toml:ro
      - ./keys.json:/app/keys.json
